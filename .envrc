# shellcheck shell=bash
# direnv: load the flake devShell (nixfmt, statix, deadnix, typos, just,
# pre-commit, …) so hooks and `just` recipes resolve their tools.
# Non-direnv users: run `nix develop` instead.
use flake
