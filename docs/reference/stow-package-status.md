# Legacy Package Status

Concrete legacy package migration status is private-host data and lives in the
private consumer repo.

Public `nix-config` keeps only generic framework assets and shared modules.
Work-specific packages, host-specific browser profiles, shell fragments, power
profiles, signing config, and service units must be moved to a private consumer
or represented here only as generic placeholders.
