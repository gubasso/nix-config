#!/usr/bin/env bash
set -euo pipefail

if command -v browser-launcher >/dev/null 2>&1; then
  exec browser-launcher brave "$@"
fi

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/browser-launcher" brave "$@"
