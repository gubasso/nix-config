# Per-host kitty overrides — included last by kitty.conf.
#
# This public copy is intentionally empty. A private consumer (nix-secrets)
# shadows this single file via the mkRealConfigDir overlay to add host-specific
# lines (e.g. the tumblesuse agents-mode SUSE launcher keybind), so no public
# file is duplicated across repos (Atomic Artifact Principle).
