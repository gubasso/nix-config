{
  "name": "${localWorkspaceFolderBasename}-sandbox",
  "image": "devimg/agents:latest",
  "postCreateCommand": {
    "pre-commit": "bash -lc 'if [ -e .envrc ] && [ -e .pre-commit-config.yaml ]; then direnv allow . && direnv exec . pre-commit install; fi'"
  }
}
