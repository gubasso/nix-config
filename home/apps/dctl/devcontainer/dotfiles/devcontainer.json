{
  "name": "${localWorkspaceFolderBasename}-sandbox",
  "mounts": [
    {
      "source": "${localEnv:HOME}/.dotfiles",
      "target": "${localEnv:HOME}/.dotfiles",
      "type": "bind"
    }
  ]
}
