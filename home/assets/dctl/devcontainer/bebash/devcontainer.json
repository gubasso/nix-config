{
  "name": "${localWorkspaceFolderBasename}-sandbox",
  "mounts": [
    {
      "source": "${localEnv:HOME}/.config/bebash",
      "target": "${localEnv:HOME}/.config/bebash",
      "type": "bind"
    }
  ]
}
