# Quick profile — gpt-5.4-mini, medium effort. Cheap fast Q&A and trivial text gen
# (codex-session ask -f, gc commit messages). Selected with `codex --profile quick`.
# Note: no gpt-5.5-mini exists yet; keep explicit 5.4-mini override.

sandbox_mode = "danger-full-access"
model = "gpt-5.4-mini"
model_reasoning_effort = "medium"
plan_mode_reasoning_effort = "medium"
