# lyra Home-Manager extras. The shared home base (modules/home/common.nix) is
# injected by mkHost; only lyra-only home config goes here.
# `username`/`hostname`/`assetsDir`/`hostSettings` arrive via extraSpecialArgs.
{ ... }:

{
  # lyra-only home.packages / programs go here.
}
