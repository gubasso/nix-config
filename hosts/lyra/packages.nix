# Host-only system packages for lyra (the quartz work machine). Add lyra-only
# tooling here (e.g. work-specific programs); orion never imports this file.
# Empty for now -- this is the per-host reuse/isolation seam, ready to fill.
{ ... }:

{
  # environment.systemPackages = [ ];
}
