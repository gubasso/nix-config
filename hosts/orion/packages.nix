# Host-only system packages for orion. The shared package base lives in
# modules/system/session.nix and the shared home modules; orion-only additions
# go here. Empty for now.
{ ... }:

{
  # environment.systemPackages = [ ];
}
