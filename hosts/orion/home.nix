# orion Home-Manager extras. The shared home base (modules/home/common.nix) is
# injected by mkHost; only orion-only home config goes here.
# `username`/`hostname`/`assetsDir`/`hostSettings` arrive via extraSpecialArgs.
{ ... }:

{
  # orion-only home.packages / programs go here.
}
