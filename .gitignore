# Throwaway VM disk images created by `system.build.vm` (modules/vm.nix).
*.qcow2

# nix build result symlinks
result
result-*

# direnv-generated devShell cache (.envrc: use flake)
.direnv/

# docs-design: drafts live outside shipped docs — never shipped, either spelling
/.draft/
/.drafts/
